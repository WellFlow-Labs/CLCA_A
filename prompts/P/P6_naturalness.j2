# CLCA Protocol v3.3 — P6 naturalness prompt
# Language: {{ lang.name }} ({{ lang.code }})

# CONTEXT (includes P1–P5 and I1)
{{ context }}

# P6 — CHANGE, CAUSATION, AND STABILITY

Analyze how {{ lang.name }} encodes the dynamics of truth-related meanings.

Explore the following types of expressions where possible:

- "X becomes [truth-related state]"
- "X becomes [non-truth / opposed state]"
- "Y causes X to be [truth-related state]"
- any other dynamic expressions involving truth-related vocabulary from the CONTEXT

### STRICT RULES:
- **Use ONLY vocabulary from CONTEXT.**
- **Auxiliary verbs, aspect markers, and function words may be added as needed.**
- **If NO COMMON EXPRESSION EXISTS, explicitly state this.**
- **Do not assume that different truth-related terms behave the same; treat each separately if needed.**

Return in subsection format similar to P4.
