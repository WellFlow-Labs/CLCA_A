# CLCA Protocol v3.4 — F4 Construction Table
# Language: {{ lang.name }} ({{ lang.code }})

# CONTEXT: P4a + P4b output
{{ context }}

Convert constructions into a table:

| Construction Type | Example | Transliteration | Translation | Usage Notes | Epistemic Status |

---

## STRICT RULES

- Do NOT summarize
- Do NOT merge or collapse entries
- Each example must be its own row
- Preserve all wording exactly
- Preserve transliteration exactly
- Preserve epistemic status exactly

- If a construction is marked:
  (no common expression identified)
  include it as a row

- Do NOT add or infer new constructions

---

Output ONLY the table
